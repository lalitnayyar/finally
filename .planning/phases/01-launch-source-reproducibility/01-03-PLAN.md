---
phase: 01-launch-source-reproducibility
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - scripts/smoke_start_command.py
  - test/specs/launch.spec.ts
  - test/specs/reproducibility.spec.ts
  - test/playwright.config.ts
  - test/package.json
  - test/package-lock.json
  - test/docker-compose.test.yml
  - backend/tests/test_routes.py
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
must_haves:
  truths:
    - "The authoritative student launch command is verified on the success path, not only through failure-path preflights."
    - "The student launch path can be verified automatically from the browser and API perspectives."
    - "The app serves the UI and API from one local origin inside the containerized runtime."
  artifacts:
    - path: "scripts/smoke_start_command.py"
      provides: "Automated success-path smoke for the documented start script"
    - path: "test/specs/launch.spec.ts"
      provides: "Same-origin launch smoke coverage"
    - path: "test/specs/reproducibility.spec.ts"
      provides: "Frontend rebuild and availability coverage"
    - path: "test/docker-compose.test.yml"
      provides: "Repeatable app plus Playwright harness"
    - path: "test/package.json"
      provides: "Pinned Playwright test dependency"
  key_links:
    - from: "test/docker-compose.test.yml"
      to: "Dockerfile"
      via: "app service build context"
      pattern: "build:\\s*context:\\s*\\.\\."
    - from: "test/specs/launch.spec.ts"
      to: "/api/health"
      via: "browser-side same-origin verification"
      pattern: "page\\.request\\.get\\('/api/health'\\)"
    - from: "test/specs/reproducibility.spec.ts"
      to: "/"
      via: "workstation availability after container build"
      pattern: "await page\\.goto\\('/'\\)"
---

<objective>
Add automated proof that the rebuilt frontend, Docker-first launch path, and same-origin runtime all work together.

Purpose: Close the Phase 01 validation gap by turning launch reproducibility and container runtime integrity into repeatable automated checks for `/gsd-execute-phase`.
Output: Launch and reproducibility Playwright specs, aligned Playwright package/image versions, and verification commands that exercise the actual student runtime path.
</objective>

<execution_context>
@/home/lnayyar/projects/CourseAIVibeCodeUdmey/finally/.codex/get-shit-done/workflows/execute-plan.md
@/home/lnayyar/projects/CourseAIVibeCodeUdmey/finally/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-launch-source-reproducibility/01-VALIDATION.md
@test/playwright.config.ts
@test/package.json
@test/docker-compose.test.yml
@test/specs/chat.spec.ts
@test/specs/connection.spec.ts
@backend/tests/market/test_factory.py
@README.md

<interfaces>
From `test/playwright.config.ts`:
```ts
export default defineConfig({
  testDir: './specs',
  timeout: 30000,
  retries: 1,
  use: {
    baseURL: process.env.BASE_URL || 'http://localhost:8000',
    headless: true,
  },
});
```

From `test/docker-compose.test.yml`:
```yaml
  playwright:
    image: mcr.microsoft.com/playwright:v1.40.0-jammy
    environment:
      - BASE_URL=http://app:8000
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add launch and reproducibility smoke coverage for the documented runtime path</name>
  <files>scripts/smoke_start_command.py, test/specs/launch.spec.ts, test/specs/reproducibility.spec.ts, backend/tests/test_routes.py</files>
  <read_first>@scripts/start_mac.sh, @scripts/stop_mac.sh, @test/specs/chat.spec.ts, @test/specs/connection.spec.ts, @test/playwright.config.ts, @README.md, @backend/tests/test_routes.py</read_first>
  <action>Create `scripts/smoke_start_command.py` as an automated harness for the authoritative student command: it should run `./scripts/start_mac.sh --build`, wait for `http://localhost:8000/` to return the rebuilt UI, call `http://localhost:8000/api/health`, assert both succeed, and then tear down with `./scripts/stop_mac.sh` even on failure. Create `test/specs/launch.spec.ts` to assert the browser can load `/`, detect key workstation shell text or landmarks from the restored source UI, and call `/api/health` from the same origin to prove `PLAT-02`. Create `test/specs/reproducibility.spec.ts` to assert the rebuilt app is reachable inside the Playwright container harness and that the root document references current generated assets instead of missing bundle paths. Extend `backend/tests/test_routes.py` only if needed for a fast API-side regression check that `/api/health` and the root static mount still coexist correctly. Do not test later-phase trading or watchlist behavior here; keep the scope on launch-path integrity. Keep the success-path smoke and the targeted Playwright spec run as separate automated checks so the app teardown in `scripts/smoke_start_command.py` does not invalidate the Playwright runtime.</action>
  <verify>
    <automated>cd /home/lnayyar/projects/CourseAIVibeCodeUdmey/finally && python3 scripts/smoke_start_command.py && cd test && docker compose -f docker-compose.test.yml run --rm playwright npx playwright test specs/launch.spec.ts specs/reproducibility.spec.ts</automated>
  </verify>
  <acceptance_criteria>An automated harness runs the documented student start command on its success path, two new launch-focused specs target the actual documented runtime path, and together they prove workstation reachability and same-origin API availability without depending on later-phase features.</acceptance_criteria>
  <done>The launch path now has browser-visible automated coverage for reproducibility and single-origin serving.</done>
</task>

<task type="auto">
  <name>Task 2: Align the Playwright harness with the containerized Phase 01 runtime</name>
  <files>test/package.json, test/package-lock.json, test/playwright.config.ts, test/docker-compose.test.yml</files>
  <read_first>@test/package.json, @test/package-lock.json, @test/playwright.config.ts, @test/docker-compose.test.yml, @.planning/phases/01-launch-source-reproducibility/01-RESEARCH.md</read_first>
  <action>Pin `@playwright/test` and the `mcr.microsoft.com/playwright` image to the same explicit version line, then update `test/package-lock.json` accordingly. Keep `BASE_URL=http://app:8000` in Docker Compose, preserve `LLM_MOCK=true` for deterministic tests, and ensure the app service still builds from the root `Dockerfile`. If necessary, raise Playwright timeouts modestly to accommodate the rebuilt frontend startup, but keep the suite under the validation budget in `01-VALIDATION.md`. The final verification command must be `cd test && docker compose -f docker-compose.test.yml up --abort-on-container-exit --exit-code-from playwright`.</action>
  <verify>
    <automated>cd /home/lnayyar/projects/CourseAIVibeCodeUdmey/finally/test && docker compose -f docker-compose.test.yml up --abort-on-container-exit --exit-code-from playwright</automated>
  </verify>
  <acceptance_criteria>The Playwright package and container image are intentionally version-aligned, the test harness runs against the rebuilt single-container app, and the full launch-focused E2E command is repeatable from the repo.</acceptance_criteria>
  <done>The Phase 01 verification harness matches the actual runtime and can prove the launch path end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser automation -> containerized app | Test clients exercise the real runtime through HTTP and rendered HTML |
| Test harness dependencies -> verification results | Package/image drift can invalidate launch confidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Tampering | Playwright image/package mismatch | medium | mitigate | Pin `@playwright/test` and `mcr.microsoft.com/playwright` to the same version line and update the lockfile together |
| T-01-09 | Repudiation | launch verification claims | medium | mitigate | Verify `/` and `/api/health` in the same automated run so launch success is observable and reproducible |
| T-01-10 | Tampering | stale generated assets in runtime image | high | mitigate, blocking | Use the Docker-built app service for the final E2E command so tests fail if the image serves missing or stale frontend assets |
</threat_model>

<verification>
- `cd test && npx playwright test specs/launch.spec.ts specs/reproducibility.spec.ts`
- `cd test && docker compose -f docker-compose.test.yml up --abort-on-container-exit --exit-code-from playwright`
</verification>

<success_criteria>
- Browser automation proves `/` renders from the rebuilt app and `/api/health` is reachable from the same origin.
- The Compose-based E2E harness runs against the root Dockerfile and rebuilt frontend.
- Playwright dependency pins are aligned and deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/01-launch-source-reproducibility/01-launch-source-reproducibility-03-SUMMARY.md`
</output>
