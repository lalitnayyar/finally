# Required for startup: OpenRouter API key for the live AI assistant
OPENROUTER_API_KEY=

# Optional: Massive (Polygon.io) API key for real market data
# Leave blank to use the built-in market simulator (default)
MASSIVE_API_KEY=

# Optional: Set to "true" only for deterministic local/testing chat responses
LLM_MOCK=false
